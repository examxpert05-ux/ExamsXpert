# -----------------------
# Server Configuration
# -----------------------
PORT=5000                     # Default port for Express server
NODE_ENV=development          # Change to "production" in production

# -----------------------
# Database Configuration
# -----------------------
# MongoDB URI for local development
MONGO_URI=mongodb://127.0.0.1:27017/examsxpert
# Uncomment below for production (replace with your cloud MongoDB URI)
# MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/examsxpert?retryWrites=true&w=majority

# -----------------------
# Authentication
# -----------------------
JWT_SECRET=change_this_super_secret   # Change to a strong secret in production
JWT_EXPIRES_IN=1d                     # Optional: token expiry duration (e.g., 1d, 12h)

# -----------------------
# Client / CORS
# -----------------------
CLIENT_URL=http://localhost:3000       # React dev URL
# For production, replace with your deployed client URL
# CLIENT_URL=https://yourdomain.com

# -----------------------
# Optional: Logging / Debug
# -----------------------
LOG_LEVEL=info        # Options: error, warn, info, debug
